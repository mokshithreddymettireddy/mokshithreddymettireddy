#include<stdio.h>
int swap(int* x,int* y);

int main()
{
  int a=12,b=36;
  swap(&a,&b);
  printf("before swapping : %d %d",a,b);
  return 0;
}
int swap(int* x ,int* y)
{
    int t;
    t=*x;
    *x=*y;
    *y=t;
     printf("after swapping : %d %d\n",*x,*y);
}
